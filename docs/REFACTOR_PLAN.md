# Skillbox Studio2 - 新项目设计方案

**跨 AI Agent 的 Skill 一键安装器** - 全新设计，参考原项目 UI 风格

---

## 项目策略

| 方面 | 决策 |
|------|------|
| **开发方式** | 🆕 从头设计新项目 |
| **核心定位** | 跨 Agent Skill 安装器 |
| **依赖** | 纯文件系统操作，无 Claude SDK |

---

## 支持的 AI Agents

| Agent | 项目级路径 | 系统级路径 |
|-------|-----------|-----------|
| Claude Code | `.claude/skills/` | `~/.claude/skills/` |
| Windsurf | `.windsurf/skills/` | `~/.codeium/windsurf/skills/` |
| Cursor | `.cursor/skills/` | `~/.cursor/skills/` |
| GitHub Copilot | `.github/skills/` | `~/.copilot/skills/` |
| Trae | `.trae/skills/` | `~/.trae/skills/` |
| Gemini CLI | `.gemini/skills/` | `~/.gemini/skills/` |
| Roo Code | `.roo/skills/` | `~/.roo/skills/` |
| Antigravity | `.agent/skills/` | `~/.gemini/antigravity/skills/` |
| Clawdbot | `skills/` | `~/.clawdbot/skills/` |
| Goose | `.goose/skills/` | `~/.config/goose/skills/` |
| OpenCode | `.opencode/skills/` | `~/.config/opencode/skills/` |
| Kilo Code | `.kilocode/skills/` | `~/.kilocode/skills/` |
| Kiro CLI | `.kiro/skills/` | `~/.kiro/skills/` |
| Amp | `.agents/skills/` | `~/.config/agents/skills/` |
| Codex | `.codex/skills/` | `~/.codex/skills/` |
| Droid | `.factory/skills/` | `~/.factory/skills/` |

---

## 用户流程设计

```mermaid
flowchart LR
    A[1️⃣ 浏览 Skill 市场] --> B[2️⃣ 选择安装范围]
    B --> C[3️⃣ 勾选目标 Agents]
    C --> D[确认安装]
```

**核心流程**：市场浏览优先 → 范围确定 → Agent 选择

---

## UI 设计 - 分页向导式

采用 **渐进式分页向导**，每一步专注一个任务：

```mermaid
flowchart LR
    S1[Step 1\n浏览市场] --> S2[Step 2\n选择范围]
    S2 --> S3[Step 3\n选择 Agents]
    S3 --> S4[确认安装]
```

---

### Step 1: 浏览 Skill 市场
```
┌─────────────────────────────────────────────────────────────┐
│  Skillbox Studio                    [设置] [关于] [🌙]      │
├───────────────────────────┬─────────────────────────────────┤
│  ● Step 1  ○ Step 2  ○ Step 3                              │
├─────────────────────────────────────────────────────────────┤
│  [搜索插件...]                      [分类: 全部 ▼]         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ☑ Plugin 1   │  │ ☐ Plugin 2   │  │ ☑ Plugin 3   │      │
│  │ 描述...      │  │ 描述...      │  │ 描述...      │      │
│  │ 3 skills     │  │ 1 skill      │  │ 5 skills     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ☐ Plugin 4   │  │ ☐ Plugin 5   │  │ ☐ Plugin 6   │      │
│  │ ...          │  │ ...          │  │ ...          │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│               已选择 2 个插件 (8 个 Skills)                  │
│                                          [下一步 →]         │
└─────────────────────────────────────────────────────────────┘
```

---

### Step 2: 选择安装范围
```
┌─────────────────────────────────────────────────────────────┐
│  Skillbox Studio                    [设置] [关于] [🌙]      │
├───────────────────────────┬─────────────────────────────────┤
│  ✓ Step 1  ● Step 2  ○ Step 3                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    选择安装范围                              │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ◉  系统级安装 (全局)                                │   │
│  │     安装到用户主目录，所有项目可用                    │   │
│  │     路径示例: ~/.claude/skills/                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ◯  项目级安装 (本地)                                │   │
│  │     仅当前项目可用                                   │   │
│  │     [📁 选择项目目录...]                             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [← 上一步]                                  [下一步 →]     │
└─────────────────────────────────────────────────────────────┘
```

---

### Step 3: 选择目标 Agents
```
┌─────────────────────────────────────────────────────────────┐
│  Skillbox Studio                    [设置] [关于] [🌙]      │
├───────────────────────────┬─────────────────────────────────┤
│  ✓ Step 1  ✓ Step 2  ● Step 3                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│           选择目标 AI Agents          [全选] [常用 3 个]    │
│                                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ ☑       │  │ ☑       │  │ ☑       │  │ ☐       │        │
│  │ Claude  │  │ Cursor  │  │ Windsurf│  │ Copilot │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ ☐       │  │ ☐       │  │ ☐       │  │ ☐       │        │
│  │ Gemini  │  │ Trae    │  │ Roo     │  │ ...更多  │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  📋 安装摘要                                                │
│  • 插件: 2 个 (8 个 Skills)                                 │
│  • 范围: 系统级                                             │
│  • Agents: Claude, Cursor, Windsurf (3 个)                 │
├─────────────────────────────────────────────────────────────┤
│  [← 上一步]                           [🚀 确认安装]         │
└─────────────────────────────────────────────────────────────┘
```

---

### Step 4: 安装进度 (可选弹窗)
```
┌───────────────────────────────────────┐
│         正在安装...                    │
├───────────────────────────────────────┤
│  ████████████░░░░░░░░  60%            │
│                                       │
│  ✓ Claude: 8/8 skills                 │
│  → Cursor: 5/8 skills                 │
│  ○ Windsurf: 等待中                   │
├───────────────────────────────────────┤
│                          [取消]       │
└───────────────────────────────────────┘
```

---

### 已安装插件管理

采用 **双重展示**：市场页轻量标记 + 独立管理标签页

#### 1️⃣ 市场页状态标记 (Step 1 内嵌)

在 Plugin 卡片上直接显示已安装徽章，并支持筛选：

```
[显示: 全部▼]  →  全部 / 已安装 / 未安装

┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ ☑ Plugin 1   │  │ ☐ Plugin 2   │  │ ✓ Plugin 3   │
│ 描述...      │  │ 描述...      │  │ [已安装 ✓]   │ ← 绿色徽章
│ 3 skills     │  │ 1 skill      │  │ 5 skills     │
└──────────────┘  └──────────────┘  └──────────────┘
```

#### 2️⃣ 独立"已安装"标签页

顶栏添加标签切换，提供详细管理功能：

```
┌─────────────────────────────────────────────────────────────┐
│  Skillbox Studio           [市场] [已安装]   [设置] [🌙]    │
├─────────────────────────────────────────────────────────────┤
│                      已安装的插件                            │
├─────────────────────────────────────────────────────────────┤
│  按 Agent 筛选:  [全部 ▼]                                   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📦 document-suite                     [卸载]        │   │
│  │  Skills: docx, pdf, pptx, xlsx                      │   │
│  │  安装于: Claude, Cursor, Windsurf                   │   │
│  │  路径: ~/.claude/skills/                            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  📦 content-harvester                  [卸载]        │   │
│  │  Skills: web-fetch, video-downloader, youtube       │   │
│  │  安装于: Claude                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  统计: Claude(12) Cursor(8) Windsurf(12)                   │
└─────────────────────────────────────────────────────────────┘
```

**功能清单**：
- 按 Agent 分组/筛选已安装的 Plugins
- 显示每个 Plugin 包含的 Skills 列表
- 显示安装路径
- 支持卸载操作（删除 skill 文件夹）

---

## 应用架构

```
┌─────────────────────────────────────────────────────┐
│                   Skillbox Studio                    │
├─────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────┐    │
│  │              Skill 市场 (主视图)             │    │
│  │  - Plugin 卡片列表                          │    │
│  │  - 搜索与分类筛选                           │    │
│  │  - 多选机制                                 │    │
│  └─────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────┐    │
│  │              安装配置面板                    │    │
│  │  - 范围选择器 (系统级/项目级)               │    │
│  │  - Agent 多选器                             │    │
│  │  - 安装按钮                                 │    │
│  └─────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────┤
│              Tauri 后端 (Rust)                       │
│  ┌─────────────────────────────────────────────┐    │
│  │  agent_config.rs  │  plugin_installer.rs    │    │
│  │  (路径配置)        │  (安装逻辑)             │    │
│  └─────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

---

## 技术栈

| 层级 | 技术 | 备注 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | 沿用原项目 |
| UI 组件 | Radix UI + Tailwind CSS 4 | 沿用原项目 |
| 动画 | Framer Motion | 沿用原项目 |
| 图标 | Lucide Icons | 沿用原项目 |
| 桌面框架 | Tauri 2 + Rust | 沿用原项目 |
| 数据源 | `Skill-Box/.claude-plugin/marketplace.json` | 本地读取 |

---

## 后端模块设计

### `agent_config.rs`
```rust
pub struct AgentConfig {
    pub id: String,           // "claude", "cursor", etc.
    pub name: String,         // "Claude Code"
    pub project_path: String, // ".claude/skills/"
    pub global_path: String,  // "~/.claude/skills/"
    pub icon: String,         // emoji or icon name
}

pub fn get_all_agents() -> Vec<AgentConfig>;
pub fn get_agent_global_path(agent_id: &str) -> PathBuf;
pub fn get_agent_project_path(agent_id: &str, project: &Path) -> PathBuf;
```

### `plugin_installer.rs`
```rust
/// 以 Plugin 为单位安装（自动包含其所有 Skills）
pub fn install_plugin(
    plugin_name: &str,
    agents: Vec<String>,      // 多个目标 Agent
    scope: InstallScope,      // Global | Project(path)
) -> Result<InstallResult>

pub fn install_plugins_batch(
    plugins: Vec<String>,
    agents: Vec<String>,
    scope: InstallScope,
) -> Result<BatchResult>

pub struct InstallResult {
    pub plugin_name: String,
    pub skills_installed: Vec<String>,
    pub agents_targeted: Vec<String>,
    pub paths_created: Vec<PathBuf>,
}
```

---

## 前端组件设计

### 从原项目参考的模块
| 原组件 | 新组件 | 参考内容 |
|--------|--------|----------|
| `PluginLibrary.tsx` | `SkillMarket.tsx` | 卡片布局、搜索、分类筛选 |
| `ui/` 目录 | `ui/` | Button, Card, Badge, Input 等 |
| `Topbar.tsx` | `Topbar.tsx` | 顶栏布局风格 |

### 新增组件
| 组件 | 功能 |
|------|------|
| `SkillMarket.tsx` | 主视图，Plugin 浏览与选择 |
| `InstallPanel.tsx` | 安装配置面板 |
| `AgentSelector.tsx` | Agent 多选器 |
| `ScopeSelector.tsx` | 范围选择器（系统/项目） |
| `InstallProgress.tsx` | 安装进度与结果 |

---

## 目录结构

```
Skillbox-Studio-new/
├── src/
│   ├── App.tsx              # 主应用（简洁）
│   ├── components/
│   │   ├── SkillMarket.tsx  # Skill 市场
│   │   ├── InstallPanel.tsx # 安装配置
│   │   ├── AgentSelector.tsx
│   │   ├── ScopeSelector.tsx
│   │   ├── Topbar.tsx
│   │   └── ui/              # 基础 UI（参考原项目）
│   ├── lib/
│   │   ├── api.ts           # Tauri 命令调用
│   │   └── agents.ts        # Agent 配置前端
│   └── styles.css
├── src-tauri/
│   └── src/
│       ├── main.rs
│       └── commands/
│           ├── agent_config.rs
│           ├── plugin_installer.rs
│           └── marketplace.rs
├── Skill-Box/               # 技能仓库
└── package.json
```

---

## 开发计划

### Phase 1: 项目初始化
1. 使用 `create-tauri-app` 初始化新项目
2. 配置 Tailwind CSS 4 + Radix UI
3. 复制原项目 `ui/` 基础组件

### Phase 2: 后端实现
1. 实现 `agent_config.rs` - Agent 路径配置
2. 实现 `marketplace.rs` - 读取 marketplace.json
3. 实现 `plugin_installer.rs` - 安装逻辑

### Phase 3: 前端实现
1. 实现 `SkillMarket.tsx` - 市场浏览
2. 实现 `InstallPanel.tsx` - 安装配置
3. 实现 `AgentSelector.tsx` + `ScopeSelector.tsx`
4. 整合主界面

### Phase 4: 完善
1. 添加安装进度反馈
2. 添加已安装状态检测
3. 测试各 Agent 路径

---

## 注意事项

> [!NOTE]
> **安装单位**：以 Plugin 为单位安装，自动复制其包含的所有 Skills。

> [!IMPORTANT]
> **路径兼容**：需同时支持 Windows (`C:\Users\`) 和 Unix (`~/`) 路径格式。

